# Build provenance

Keep one copy of this record with every package that is flashed. The
repository intentionally does not store firmware binaries.

```text
Target hardware: IPC_GK7205V200_G5C-LQ_S38
Target DevID:    000659O61001000000000200
Stock firmware SHA256:
Stock InstallDesc SHA256:
OpenIPC repository:
OpenIPC commit:
OpenIPC configuration/defconfig:
python-dvr repository:
python-dvr commit:
uImage.gk7205v200 SHA256:
rootfs.squashfs.gk7205v200 SHA256:
XM package SHA256:
Build timestamp (UTC):
Hardware test result:
```

Do not publish camera passwords, CheckCodes, session identifiers, or packet
captures containing unrelated private traffic.
